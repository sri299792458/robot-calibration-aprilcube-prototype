schema_version: 1

# Explicit directed taught-pose route. Reverse edges are separate approvals.
# Per-run __handoff__ entry/return edges are derived from the live stationary
# Ready state and collision-validated before a command publisher is created.
edges:
  - [pose_001, pose_002]
  - [pose_002, pose_001]
