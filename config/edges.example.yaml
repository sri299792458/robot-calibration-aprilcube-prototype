schema_version: 1

# Explicit directed execution route. Reverse edges are separate approvals; add
# every edge that the commissioning or collection plan will actually use.
edges:
  - [home, pose_001]
  - [pose_001, home]
  - [pose_001, pose_002]
  - [pose_002, home]
