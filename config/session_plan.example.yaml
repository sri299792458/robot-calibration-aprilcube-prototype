schema_version: 1
home_pose_id: home

# Replace with the exact manually recorded order. The path-validation edge file
# must contain every adjacent directed edge plus the final return-to-home edge.
capture_pose_ids:
  - home
  - pose_001
  - pose_002
  # Revisit anchors throughout the run to detect camera/cube mount movement.
  - home
