schema_version: 2

# Replace with the exact manually recorded order. The policy edge file contains
# adjacent taught-pose edges. Live __handoff__ entry/return edges are added and
# validated automatically for each run.
capture_pose_ids:
  - pose_001
  - pose_002
  # Revisit anchors throughout the run to detect camera/cube mount movement.
  - pose_001
